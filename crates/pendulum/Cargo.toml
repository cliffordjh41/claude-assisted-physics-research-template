[package]
name = "pendulum"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Plane pendulum: equation of motion, energy, and phase-space trajectory via the numerics crate's velocity-Verlet integrator."

[dependencies]
numerics = { path = "../numerics" }
