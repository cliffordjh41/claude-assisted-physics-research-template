[package]
name = "lab-core"
version = "0.0.1"
edition.workspace = true
license.workspace = true

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2"
gravity = { path = "../gravity" }
pendulum = { path = "../pendulum" }
