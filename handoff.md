# Handoff

## Open line
Fresh directory. Discipline layer, safety hooks, and the Rust -> wasm ->
browser lab stack are in place; no research started.

`library/` is empty of sources and is where they go, filed by discipline per
`library/CLAUDE.md`. It is checked before searching the web for a source, and
`python3 tools/index_library.py` regenerates the index that makes checking it
one grep. `lean/example/` builds and carries the pattern for machine-checking
the algebra a result rests on; its first build downloads Mathlib.

## Next task
Awaiting first instruction. On session start, read CLAUDE.md, the always-on
rules in .claude/rules/, this file, and the current journal volume, then
state current state and wait for direction -- including which stage the
work enters at. A picture with no formal shape yet enters at stage 0
(sketch/), not at stage 1.

## Pointer
Last journal entry: none yet.
